[package]
name = "app"
version = "0.1.0"
edition = "2021"

[dependencies]
pico-sdk = { workspace = true }
rsp-client-executor = { workspace = true }
bincode = "1.3.3"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
debug = true
embed-bitcode = true
